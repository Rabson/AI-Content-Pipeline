{
  "name": "@aicp/shared-config",
  "version": "0.1.0",
  "private": true,
  "main": "env/readers.js",
  "types": "env/readers.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"env/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --dir ."
  }
}